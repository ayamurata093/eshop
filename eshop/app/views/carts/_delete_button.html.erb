<%= form_for(user.carts.find_by(product_id: product.id), html: { method: :delete }) do |f| %>
  <%= hidden_field_tag :prodcut_id, product.id %>
  <%= f.submit 'カートから削除', class: 'btn btn-danger btn-xs' %>
<% end %>